[package]
name = "metadata"
version = "0.1.0"
edition = "2021"
description = "Schema e (de)serializzazione del sidecar non distruttivo di RawForge (docs/ARCHITECTURE.md, §3.1)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
