[workspace]
resolver = "2"
members = [
    "core-types",
    "color-science",
    "xmp",
    "harmonic",
    "smartbatch",
    "metadata",
    "gpu-pipe",
    "raw-decode",
    "ffi",
]

# raw-decode usa `rawler` (puro Rust) invece di LibRaw (C++): vedi il
# commento di testa in raw-decode/src/lib.rs per il perché di questa scelta
# rispetto a quanto originariamente descritto in docs/ARCHITECTURE.md §1.1.
#
# Prossimi crate (Fase 1 della roadmap, vedi docs/ARCHITECTURE.md):
# "cache", "catalog", "job-scheduler"
